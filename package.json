{
  "name": "pet-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rongrong Paradise - a runnable cat-focused mini-game prototype.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
